

<h1>Bookings</h1>

<div id="bookings">
  <% @bookings.each do |booking| %>

   <p>booking: <%= booking.start %></p>
   <p>Capacity: <%= booking.end %></p>
   <p>booking: <%= booking.purpose %></p>
   <p>Capacity: <%= booking.room.name %></p>
    <p>
      <%= link_to "Show this booking", booking %>
    </p>
  <% end %>
</div>

<%= link_to "New booking", new_booking_path %>